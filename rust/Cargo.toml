[package]
name = "rust"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
godot = {version = "0.2.4", features = ["register-docs"]}
ron = "0.9"
serde ={ version = "1", features = ["derive"]}
